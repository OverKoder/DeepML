[project]
name = "deepml"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bitsandbytes>=0.49.2",
    "unsloth>=2026.6.9",
]
[tool.uv]
exclude-dependencies = [
    "torch",
    "torchvision",
    "torchaudio",
]
